# .gitignore for a Next.js Project

# Dependencies
# This folder can get massive and can always be regenerated by running `npm install` or `yarn`
/node_modules/

# Next.js build outputs
# This is the compiled version of your app, which gets created every time you build.
/.next/
/out/

# Production build
# Another build output folder.
/build

# Environment variables
# CRITICAL! This is where you store secret keys. NEVER commit these to GitHub.
.env.local
.env.development.local
.env.test.local
.env.production.local
.env.*.local

# Log files
# Various log files that can be generated during development.
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# System files
# Annoying OS-specific files that have no place in a repo.
.DS_Store
Thumbs.db

# VS Code specific
# Ignore user-specific VS Code settings, but allow recommended extensions and settings.
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json

# Caches
# Caching for tools like SWC (Next.js's compiler) and Turbopack.
.swc/
.turbo/

# Test outputs
# Coverage reports from testing libraries like Jest.
/coverage/

# Static assets from a CMS (optional)
/public/assets

